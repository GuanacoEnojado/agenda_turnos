import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CambiodeturnoPage } from './cambiodeturno.page';

describe('CambiodeturnoPage', () => {
  let component: CambiodeturnoPage;
  let fixture: ComponentFixture<CambiodeturnoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CambiodeturnoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
